import { Typography } from "@mui/material";

const LandingPage = () => {
  return <div className="w-full max-h-screen m-auto animate-pulse">
    <div className="m-5 text-center flex flex-col items-center">
      <Typography variant="h5" color='white'>Loading page</Typography>
    </div>
  </div>;
};

export default LandingPage;
